######################################################
# Pool Cleaner Schedule
######################################################
# By default, this component is executed once per day
# during the night.
#
# It should be disabled in a multi-server set-up,
# to avoid deadlocks and so on.
######################################################

######################################################
# Enable or disable service
######################################################
# By default it is enabled. In a multi-server
# environment, the service should only be enabled on
# on emachine.  Turn it off by setting the property
# to false:
#
# Example:
#   serviceEnabled = fakse
#
# Default:
#   serviceEnabled = true
######################################################
# serviceEnabled = true

