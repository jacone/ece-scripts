# This will run the DeleteArticlesService at the specified
# time. Default is at 04:00 every night. See
# /neo/io/services/DeleteArticlesService for configuration of the
# service. Add to Initial.properties to activate.

hour=4

